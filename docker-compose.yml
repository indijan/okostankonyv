services:
  okostankonyv:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: okostankonyv
    restart: unless-stopped
    ports:
      - "3010:3000"
    env_file:
      - .env.production
